// prisma/schema/content.prisma

model Notice {
  id        String   @id @default(uuid())
  title     String
  content   String   @db.Text
  active    Boolean  @default(true)
  date      DateTime @default(now())

  coachId   String?
  coach     User?    @relation("CoachNotices", fields: [coachId], references: [id], onDelete: Cascade)
}

model Content {
  id          String   @id @default(uuid())
  title       String
  subtitle    String?
  description String?  @db.Text
  
  category    String   
  
  type        String   @default("video")
  isVIP       Boolean  @default(false)

  // 🔥 Preço de venda avulsa (PIX/cartão) via Biblioteca — só relevante quando
  // isVIP=true e o aluno não tem ContentAccess. Null/0 = não é vendável (só
  // libera manualmente pelo admin, ex: aula VIP de brinde).
  valor       Float?

  videoUrl    String?
  pdfUrl      String?
  audioUrl    String?
  thumbUrl    String?
  duration    String?

  createdAt   DateTime @default(now())

  coachId     String?
  coach       User?    @relation("CoachContents", fields: [coachId], references: [id], onDelete: Cascade)

  likes       ContentLike[]
  comments    ContentComment[]
  completions ContentCompletion[]
  unlockedFor ContentAccess[]
  purchases   Payment[]
}

model ContentLike {
  id        String   @id @default(uuid())
  
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  contentId String
  content   Content  @relation(fields: [contentId], references: [id], onDelete: Cascade)
  
  createdAt DateTime @default(now())

  @@unique([userId, contentId])
}

model ContentComment {
  id        String   @id @default(uuid())
  text      String   @db.Text
  
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  contentId String
  content   Content  @relation(fields: [contentId], references: [id], onDelete: Cascade)
  
  createdAt DateTime @default(now())

  parentId  String?
  parent    ContentComment?  @relation("CommentReplies", fields: [parentId], references: [id], onDelete: Cascade)
  replies   ContentComment[] @relation("CommentReplies")
}

model ContentCompletion {
  id        String   @id @default(uuid())
  
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  contentId String
  content   Content  @relation(fields: [contentId], references: [id], onDelete: Cascade)
  
  createdAt DateTime @default(now())

  @@unique([userId, contentId]) 
}

model ContentAccess {
  id        String   @id @default(uuid())
  
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  contentId String
  content   Content  @relation(fields: [contentId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([userId, contentId]) 
}

// =============================================================================
// 🔥 ADICIONADO: MÓDULO DE PRODUTOS DIGITAIS, E-BOOKS E ORDER BUMP
// =============================================================================

model ProdutoDigital {
  id              String   @id @default(uuid())
  slug            String   @unique
  nome            String
  descricao       String?  @db.Text
  capaUrl         String?
  valor           Float
  ativo           Boolean  @default(true)
  coachId         String   // Paulo ou Adri
  linkEntrega     String?  // Link do Google Drive, PDF do e-book, etc.
  
  // Estrutura premium para o checkout com Order Bump nativo
  orderBumpTitulo String?  // Ex: "Levar também o Guia Avançado de Glúteos"
  orderBumpTexto  String?  @db.Text // Ex: "Acelere os resultados combinando com treinos de alta densidade."
  orderBumpValor  Float?   // Preço adicional estrategicamente menor

  vendas          ProdutoVenda[]

  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}

model ProdutoVenda {
  id              String   @id @default(uuid())
  produtoId       String
  produto         ProdutoDigital @relation(fields: [produtoId], references: [id], onDelete: Cascade)
  
  nomeCliente     String
  emailCliente    String
  telefoneCliente String
  cpfCliente      String
  
  status          String   @default("PENDENTE") // PENDENTE, PAGO
  valorTotal      Float    // Armazena o valor consolidado final cobrado no Asaas
  incluiuBump     Boolean  @default(false)
  
  asaasPaymentId  String?  @unique
  asaasCustomerId String?
  pixQrCode       String?  @db.Text
  pixCopyPaste    String?  @db.Text
  invoiceUrl      String?  @db.Text

  paymentDate     DateTime?
  
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}